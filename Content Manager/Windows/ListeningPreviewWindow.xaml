<Window x:Class="Content_Manager.Windows.ListeningPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Content_Manager.Windows"
        mc:Ignorable="d"
        Title="ListeningPreviewWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="GreenButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Button.Width" Value="120"/>
            <Setter Property="Button.Height" Value="30"/>
            <Setter Property="Button.FontFamily" Value="Verdana"/>
            <Setter Property="Button.FontSize" Value="14"/>
            <Setter Property="Button.FontWeight" Value="Normal"/>
            <Setter Property="Button.Foreground" Value="White"/>
            <Setter Property="Button.Background" Value="DarkGreen"/>
            <Setter Property="Button.BorderBrush" Value="Black"/>
        </Style>
    </Window.Resources>
    <Grid>
        
        <StackPanel Orientation="Horizontal">
            <Button Style="{StaticResource GreenButtonStyle}" x:Name="btnPlay" Content="Play" Click="btnPlay_Click"></Button>
            <Button Style="{StaticResource GreenButtonStyle}" x:Name="btnPause" Content="Play" Click="btnPause_Click"></Button>
            <Button Style="{StaticResource GreenButtonStyle}" x:Name="btnStop" Content="Play" Click="btnStop_Click"></Button>

        </StackPanel>
    </Grid>

</Window>
