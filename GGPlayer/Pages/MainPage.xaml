<Page x:Class="GGPlayer.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GGPlayer.Pages" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Images/fon.jpg" Stretch="UniformToFill"/>
        </Grid.Background>

        <Viewbox Stretch="Uniform">
            <Grid Width="1000" Height="600">
                <Image Source="/Images/b1ow.png" Stretch="Uniform" 
                       HorizontalAlignment="Left" VerticalAlignment="Top"
                       Width="150" Height="80" Margin="230,0,0,0"/>

                <Image Source="/Images/lam.png" Stretch="Uniform" 
                       HorizontalAlignment="Left" VerticalAlignment="Top"
                       Width="580" Height="300" Margin="200,10,0,0"/>
                
                <Image Source="/Images/guo.png" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0, 70, 0, 0"
                       Width="900" Height="600"/>

                <Image Source="/Images/mazhdig.png" Stretch="Uniform"
                       HorizontalAlignment="Left" VerticalAlignment="Top"
                       Width="500" Height="400" Margin="450,280,0,0"/>

                <ListView x:Name="lvSegments" ItemsSource="{Binding Segments}"
                          MouseDoubleClick="lvSegments_MouseDoubleClick"
                           Background="Transparent"
                          BorderThickness="0" 
                          BorderBrush="Transparent"
                          HorizontalAlignment="Left" VerticalAlignment="Top"
                          Width="500" Height="300" Margin="50,100,0,0">
                    <ListView.Resources>
                        <!-- Стили для элементов ListView -->
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Grid>
                                            <ContentPresenter/>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.Resources>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="400" Height="30">
                                <Image Source="/Images/nuyda.png" Stretch="Fill"/>
                                <TextBlock Text="{Binding Title}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           FontWeight="Bold"
                                           FontSize="18"
                                           Foreground="White"
                                           TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </Grid>
        </Viewbox>
    </Grid>
</Page>